import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  starIcon: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    padding: 10,
  },
  energy_level: {
    padding: 10,
  },
});

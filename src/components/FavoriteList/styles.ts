import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  favorite_container: {
    marginTop: 10,
  },

  stretch: {
    width: '98%',
    height: 300,
    margin: 'auto',
    borderRadius: 10,
  },
});

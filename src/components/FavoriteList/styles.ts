import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  favorite_container: {
    borderBottomWidth: 2,
    borderBottomColor: '#608cf5',
    padding: 20,
  },

  stretch: {
    width: '100%',
    height: 300,
    borderRadius: 10,
    resizeMode: 'cover',
  },
});

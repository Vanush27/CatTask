import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 20,
  },

  textHeader: {
    fontSize: 30,
    textAlign: 'center',
    color: '#3a6ff3',
  },
});
